import 'package:flutter/cupertino.dart';
import 'package:flutter/material.dart';

import 'widgets/on_bording_view_body.dart';

class On_Bording_View extends StatelessWidget {
  const On_Bording_View({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: On_Bording_View_Body(),
    );
  }
}
